API_HOST=127.0.0.1
API_PORT=8787

LLM_PROVIDER=openai-compatible
LLM_MODEL=qwen-plus
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=

EMBEDDING_PROVIDER=bailian
EMBEDDING_MODEL=text-embedding-v4
EMBEDDING_DIM=1024
EMBEDDING_OUTPUT_TYPE=dense
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=

# Agent execute_command sandbox: Python venv with numpy/pandas/matplotlib/scikit-learn.
# Default auto-detects repo `.venv` (also checks npm INIT_CWD). Override with absolute path:
# WORKSPACE_PYTHON_VENV=/path/to/DataFoundry/.venv
# WORKSPACE_COMMAND_TIMEOUT_MS=120000

STORAGE_ROOT_DIR=storage
METADATA_DB_PATH=storage/metadata/workbench.sqlite
SECRET_MASTER_KEY=replace-with-a-long-random-local-key

# Auth mode (independent of `npm run dev` vs production build):
#   dev      — local default. Auto sign-in as dev-user via X-Dev-Token; no /login page.
#   password — cookie session + CSRF; requires register/login. Production default when NODE_ENV=production.
#
# `npm run dev` only controls hot-reload; it does NOT pick auth mode. To test login locally,
# set both DATAFOUNDRY_AUTH_MODE=password here and NEXT_PUBLIC_DATAFOUNDRY_AUTH_MODE=password
# in apps/web/.env.local, then restart dev servers.
DATAFOUNDRY_AUTH_MODE=dev
#
# Required only when DATAFOUNDRY_AUTH_MODE=password:
AUTH_SESSION_SECRET=replace-with-at-least-32-random-characters
AUTH_PUBLIC_BASE_URL=https://datafoundry.example.com
# test = log verification/reset links to console; smtp = send real email.
AUTH_EMAIL_DELIVERY=smtp
AUTH_EMAIL_FROM=DataFoundry <no-reply@example.com>
AUTH_SMTP_HOST=smtp.example.com
AUTH_SMTP_PORT=587
AUTH_SMTP_SECURE=false
AUTH_SMTP_USER=
AUTH_SMTP_PASSWORD=

SQL_DEFAULT_LIMIT=100
SQL_MAX_LIMIT=1000
SQL_TIMEOUT_MS=10000
